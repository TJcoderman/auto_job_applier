version: "3.10"

services:
  agentic-job-hunter:
    build: .
    container_name: agentic-job-hunter
    ports:
      - "${PORT:-8000}:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
      - ./config:/app/config
    restart: unless-stopped

